# CLAUDE.md — 이 저장소에서 일하는 규칙

## 0. 세션을 시작하면 이 순서로 읽는다

1. `docs/roadmap.md` — 안정화 판단 기준 표 · 가정 표. **지금 어디까지 왔는지가 여기 있다.**
2. `docs/08-feedback.md` — 열린 이슈
3. `docs/adr/` — 되돌리기 비싼 결정과 그 이유

**문서가 기억이다.** 세션은 기억하지 못하고 문서는 기억한다.

---

## 1. 기억해 둘 것 — 이 앱의 뼈대

- **파일이 원본이다.** 사용자 폴더의 `.md` 와 이미지가 유일한 진실이다.
  앱 DB 는 없다. 본문을 어딘가에 복사해 두는 코드를 쓰면 ADR-0001 을 어긴 것이다.
- **색인은 캐시다.** `Application Support` 의 SQLite FTS5 는 언제 지워도 된다.
  마이그레이션을 쓰지 않는다 — 버전이 다르면 통째로 지우고 다시 만든다.
- **편집기의 문자열은 언제나 마크다운 원문 하나다.** 보이는 모습의 차이는 전부
  속성(글꼴 · 색 · 마커 숨김)으로 낸다. 저장은 `textStorage.string` 을 그대로 쓴다.
  두 문자열을 매핑하는 코드가 생기면 ADR-0005 를 어긴 것이다.
- **키보드 툴바(`placement: .keyboard`)를 쓰지 않는다.** 키보드 위 도구는
  `.safeAreaInset(edge: .bottom)` 으로 화면 안에 그린다.
- **글꼴 · 폭을 숫자로 못 박지 않는다.** `Font.scaled` · `scaledLength` 하나로 통한다.
- **값의 출입구는 하나다.** 본문을 고치는 길은 편집기 하나뿐이다.
- **모든 삭제에 확인.** 삭제는 폴더 안 `.trash/` 로 옮기고, 영구 삭제는 타이핑 확인.

---

## 2. 원격 세션의 제약 — 여기서 컴파일할 수 없다

이 세션은 리눅스 컨테이너다. **Xcode 도 Swift 툴체인도 없고 `download.swift.org` 는
네트워크 정책으로 막혀 있다** (2026-09-13 실측: `CONNECT tunnel failed, 403`).
설치하려 하지 마라 — 지난 앱에서도 같은 결론이었다.

그래서 검증 사다리는 이렇게 된다:

| 심판 | 잡는 것 | 어디서 | 비용 |
|---|---|---|---|
| **파이썬 대조** (`Tools/golden/`) | 마크다운 파싱 · 링크 추출 · 머리말 기댓값 | **여기서 직접** | 초 |
| `Core swift test` | 순수 로직 | CI 리눅스 러너 (`core-test.yml`) | 2~4분 |
| 앱 빌드 | 문법 | CI macOS 러너 (`build.yml`) | 10분 |
| 시뮬레이터 스크린샷 | 화면 | CI macOS 러너 | 15분 |
| 실기기 TestFlight | iCloud · 문서 피커 · 사진 권한 · 한글 조합 | 사람 | 30분 + 사람 |

**여기서 열어볼 수 없는 주소가 있다.** 네트워크 정책이 막는다 — GitHub Pages
(`helpnara.github.io`)도 그중 하나다 (2026-09-13 실측: `connect_rejected`).
`site.yml` 이 초록이어도 **페이지가 실제로 보이는지는 사람이 봐야 한다.**
워크플로 로그의 `Evaluated environment url` 이 우리가 아는 전부다.

**규칙.**

- 여기서 돌릴 수 있는 것(`python3 Tools/golden/generate.py --check`)을 **다 돌린 뒤에** 푸시한다.
- Swift 테스트의 기댓값을 손으로 적지 않는다. `Tools/golden/` 이 파이썬으로 계산한
  JSON 만 쓴다. 손으로 적은 기댓값은 **테스트가 버그를 승인**하게 만든다.
- SwiftUI 코드는 CI 로그가 첫 심판이다. 보수적으로 쓴다.
- 실기기에서 뭔가 잡히면 **"어느 칸에서 잡았어야 했나"** 를 묻고 그 칸에 심판을 세운다.

---

## 3. 빌드 규율

1. **빌드는 "착수하자" 에만.** 이슈는 `docs/08-feedback.md` 에 번호 · 🔴🟡🔵🟢🟠 · 빌드 번호로 쌓는다.
   **하나 고치자고 빌드하지 않는다 — 5개 내외로 묶어** "착수할까요?" 를 묻는다
   (2026-09-16 사용자 규칙). 자료 유실 · 저장 실패만 예외로 바로 고친다.
2. **빌드마다 두 목록을 준다.** (앱 안에서) 확인 1~n · (콘솔 · 사이트에서) 할 일.
   사용자는 "1~n 확인 완료" 로 답한다.
   **확인할 것은 앱이 실제로 하는 일로, 손가락이 할 동작으로 적는다** — *마땅히 그래야 할 일*
   로 적으면 사람이 확인해 줄 수 없거나 **없는 기능을 확인해 준 셈**이 된다 (빌드 35 · 23번).
   **새 동작을 적기 전에 그 자리의 코드를 한 번 읽는다** — 규칙을 알고도 빌드 47 · 4번에서
   또 걸렸다. 아는 것으로는 안 되고 **절차**가 있어야 한다.
   우리끼리 쓰는 말(마커 · 울타리 · 렌더)을 쓰지 않는다 (빌드 29 · 5번 사용자).
3. **큰 변경 뒤 첫 확인은 "껐다 켜서 그대로인가"** — 자동 저장 · 색인 · 북마크.
   "없어진 것" 은 스크린샷에 안 나온다.
4. **같은 자리에서 CI 가 두 번 죽으면 러너 탓이 아니라 내 코드다.**
   같은 자리에서 **사용자가 두 번 걸리면 반만 고친 것**이다 (139 → 141). 고치기 전에
   **경우의 수를 표로 깔고** 심판에 물린다 (2026-09-18 사용자). 한 사례를 고치는 것은
   고친 것이 아니다 — 마커 · 단계 · 여러 줄 · 빈 줄 · 거듭 누르기를 곱해 본다.
5. 앱이 죽으면 **설정 → 개인정보 보호 → 분석 데이터** 의 크래시 로그부터 받는다.
6. 푸시 전 `git pull --rebase` — CI 가 `screenshots/` 를 커밋한다.
7. 세션 끝: `docs/08-feedback.md` 상태 갱신 · 확인된 가정에 날짜 · 다음 세션 첫 줄에
   "지금 어디까지" 한 줄.

---

## 4. 코드 경계

- `Packages/Core` 는 **`import SwiftUI` · `import UIKit` · `import SQLite3` 를 하지 않는다.**
  이 경계가 무너지면 리눅스 `swift test` 가 통째로 없어진다.
- SQLite 호출은 `App/Search/` 에만. Core 는 질의 문자열까지만 만든다.
- 파일 I/O 는 `actor` 안에서. 화면에는 `Sendable` 구조체만 건넨다. `@preconcurrency` 금지.
- 한글 파일명은 **읽는 즉시 NFC 로 정규화**한다 (`Paths.normalized`).
  iCloud · Files · 옵시디언 사이에서 NFD 로 오가기 때문이다.

---

## 5. CI 워크플로 규칙

**이 저장소는 public 이다** (2026-09-13 사용자 결정). 그래서 macOS 러너가 무료다 —
빌드를 아끼려고 검사를 빼지 않는다. 대신 `docs/` 가 전부 공개되므로 **실제 지원
메일 · 시크릿 · 개인 정보를 문서에 적지 않는다.**


- `paths-ignore` 로 문서만 고친 푸시는 건너뛴다.
- `concurrency` + `cancel-in-progress`.
- 커밋하는 워크플로에는 `permissions: contents: write`.
- **아티팩트 업로드를 커밋보다 앞에** 둔다 — 커밋이 실패하면 결과물을 못 받는다.
- 릴리스 노트의 한글은 `\u` 이스케이프.
- 소스 안의 안내 문구 강조는 `**…**`. **곧은 따옴표(`"`)를 문구 안에 쓰지 않는다** —
  지난 앱에서 빌드 시스템이 같은 자리에서 두 번 죽었다.
